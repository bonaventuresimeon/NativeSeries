Thank you for installing student‑tracker!

Your release name is: {{ .Release.Name }}

To check the application status:

  kubectl --namespace {{ .Release.Namespace }} get pods

If Ingress is enabled, access the service at:

  http://{{ (index .Values.ingress.hosts 0).host }}
