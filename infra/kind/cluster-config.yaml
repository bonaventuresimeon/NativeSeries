apiVersion: kind.x-k8s.io/v1alpha4
kind: Cluster
name: simple-cluster
nodes:
- role: control-plane
  extraPortMappings:
  - containerPort: 8011
    hostPort: 8011
    protocol: TCP
- role: worker
- role: worker